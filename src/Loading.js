import React from 'react';
import  './Loading.css'
const Loading = ()=>{
    return (
        <div>
            <i className=" icon-middle massive asterisk loading icon"></i>
        </div>

    );

};

export default Loading;